# given a text file and a list of string, find the max length of strings that combined from the given list of string in the text file. Each character in the given list of string can be used only once.
#找字典里只有一个相同字母的两个字符串的最大乘积
#实现一个family tree,每个node有多个parent多个children,这个parent tree里面可能有环,除了constructor destructor,要实现一个CommonAncestor(node1,node2)函数,判断两个node是否是亲戚
#思路,应该是一个DAG，但是又有环又有commonancestor,不懂
'''
1.给两个四分树，求两个图重叠的1的个数
2. 怎么continuous deploy
3. run length representation的合并
‘’‘
